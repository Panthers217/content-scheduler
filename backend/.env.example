# MongoDB connection string
MONGODB_URI=mongodb://localhost:27017/content_scheduler

# Firebase Admin SDK service account (JSON) path or the JSON content
FIREBASE_SERVICE_ACCOUNT=./serviceAccountKey.json

# Port for backend
PORT=4000
