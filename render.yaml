services:
  - type: web
    name: content-scheduler-backend
    runtime: node
    plan: free
    buildCommand: cd backend && npm install
    startCommand: cd backend && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        generateValue: true
      - key: MONGODB_URI
        value: mongodb+srv://marlostudioa_db_user:WH2sRvZO0sr7sXwa@cluster0.wtofznr.mongodb.net/Cluster0?retryWrites=true&w=majority
    healthCheckPath: /health